version: '3.1'

services:

  db:
    image: mariadb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: identity
      MYSQL_USER: maria
      MYSQL_PASSWORD: pwd4maria

    volumes:
      - ./init:/docker-entrypoint-initdb.d

    ports:
      - 3306:3306

#  adminer:
#    image: adminer
#    restart: always
#    ports:
#      - 8080:8080